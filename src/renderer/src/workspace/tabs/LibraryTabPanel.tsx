/**
 * 库管理 Tab 面板（薄壳）
 *
 * 实际实现见 workspace/library/view/CavityLibraryPanel.tsx。
 * 保持 buildComponents / tabTypeRegistry 的接入点不变（library 面板懒加载）。
 */
export { default } from '../library/view/CavityLibraryPanel'
